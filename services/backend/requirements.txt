# Backend - PR Agent Demo core service
# Python 3.12+

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Database
motor>=3.7.0
pymongo>=4.10.0

# Configuration
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Authentication (stage 7: JWT + bcrypt)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 relies on bcrypt internals removed in bcrypt 4.1+
bcrypt>=4.0.1,<4.1

# LLM (stage 2: LangChain + LangGraph)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0
langgraph-checkpoint-mongodb>=0.4.0

# Task queue (stage 9: durable pipeline execution)
arq>=0.28.0

# MCP Client
mcp>=1.0.0

# Utilities
python-multipart>=0.0.12
beautifulsoup4>=4.12.0
curl_cffi>=0.7.0
cloudscraper>=1.2
feedparser>=6.0
