# Frontend Dev Dockerfile — Vite 开发服务器
FROM node:22-alpine

WORKDIR /app

# 依赖层缓存
COPY package.json package-lock.json* ./
RUN npm ci

# 应用代码通过 volume 挂载（热重载）
COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
