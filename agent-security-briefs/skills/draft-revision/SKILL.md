---
name: draft-revision
description: Create an immutable child draft version from an explicit parent version and bounded revision instruction.
version: 2.0.0
---
# Draft revision

Bind every edit to an immutable parent artifact and expected version. Preserve non-target content, create a new version, run review, and cap automatic repair attempts. Never overwrite the parent or switch the current primary draft without explicit confirmation.
